mode: standalone  # or distributed for multi-node setup
persistence:
  size: 10Gi
resources:
  requests:
    memory: 1Gi

service:
  type: ClusterIP  # or LoadBalancer/NodePort based on your needs
replicas: 4  # Number of MinIO instances for distributed mode